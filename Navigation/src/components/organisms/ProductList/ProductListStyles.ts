import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    empty: {
        alignItems: 'center',
        paddingTop: 40,
    },
    emptyText: {
        color: '#9CA3AF',
        fontSize: 14,
    },
});

export default styles;