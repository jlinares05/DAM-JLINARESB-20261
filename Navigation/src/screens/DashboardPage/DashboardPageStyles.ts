import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    actions: {
        marginBottom: 14,
    },
    sectionLabel: {
        marginBottom: 4,
    },
});

export default styles;